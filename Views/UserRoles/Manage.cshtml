@model List<ManageUserRolesViewModel>
@{
    ViewData["Title"] = "Manage";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<form method="post">
    <div class="card">
        <div class="card-header">
            <h2>Manage User Roles</h2>
            Add/Remove Roles for User @ViewBag.UserName.
        </div>
        <div class="card-body">
            @for (int i = 0; i < Model.Count; i++)
            {
                <div class="form-check m-1">
                    <input type="hidden" asp-for="@Model[i].RoleId" />
                    <input type="hidden" asp-for="@Model[i].RoleName" />
                    <input asp-for="@Model[i].Selected" class="form-check-input" />
                    <label class="form-check-label" asp-for="@Model[i].Selected">
                        @Model[i].RoleName
                    </label>
                </div>
            }
            <div asp-validation-summary="All" class="text-danger"></div>
        </div>
        <div class="card-footer">
            @using (Html.BeginForm())
            {
                <input type="hidden" name="returnUrl" value="@ViewBag.refer" />
                <input type="hidden" name="UserId" value="@ViewBag.userId" />
                <button asp-controller="UserRoles" asp-action="Manage" asp-route-previous="" class="btn btn-primary" type="submit">Update</button>
                <a href="@ViewBag.Refer"
                   class="btn btn-primary" style="width:auto">Cancel</a>
            }
        </div>
    </div>
</form>